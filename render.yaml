services:
  - type: web
    name: nuvio-live-sports-addon
    runtime: node
    plan: free
    buildCommand: npm install --omit=dev
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: PPV_ENABLED
        value: true
      - key: ROXIE_ENABLED
        value: true
      - key: ROXIE_BASE_URL
        value: https://roxiestreams.info
      - key: ROXIE_EVENT_MAP_JSON
        value: '[]'
      - key: CACHE_TTL_SECONDS
        value: 60
